import { protectAdminPage } from "./admin-auth.js";import { db,collection,addDoc,getDocs,deleteDoc,doc,serverTimestamp } from "./firebase-service.js";protectAdminPage();const zoneForm=document.getElementById("zoneForm"),zoneName=document.getElementById("zoneName"),zoneFee=document.getElementById("zoneFee"),zonesTable=document.getElementById("zonesTable");function formatNaira(a){return new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0}).format(a||0)}async function loadZones(){const snap=await getDocs(collection(db,"delivery_zones"));zonesTable.innerHTML="";snap.forEach(ds=>{const z=ds.data();zonesTable.innerHTML+=`<tr><td>${z.zone}</td><td>${formatNaira(Number(z.fee||0))}</td><td><button class="deleteBtn" onclick="deleteZone('${ds.id}')">Delete</button></td></tr>`})}zoneForm.addEventListener("submit",async e=>{e.preventDefault();await addDoc(collection(db,"delivery_zones"),{zone:zoneName.value.trim(),fee:Number(zoneFee.value),createdAt:serverTimestamp()});zoneForm.reset();loadZones();alert("Delivery zone saved.")});window.deleteZone=async id=>{if(!confirm("Delete this delivery zone?"))return;await deleteDoc(doc(db,"delivery_zones",id));loadZones()};loadZones().catch(e=>alert(e.message));
