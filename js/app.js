let cart=[];let currentCategory="all";const grid=document.getElementById("productGrid");const cartItems=document.getElementById("cartItems");const cartTotal=document.getElementById("cartTotal");const searchInput=document.getElementById("searchInput");function formatNaira(amount){return new Intl.NumberFormat("en-NG",{style:"currency",currency:"NGN",maximumFractionDigits:0}).format(amount)}function renderProducts(){const search=searchInput.value.toLowerCase();const filtered=products.filter(product=>{const matchCategory=currentCategory==="all"||product.category===currentCategory;const matchSearch=product.name.toLowerCase().includes(search);return matchCategory&&matchSearch});grid.innerHTML=filtered.map(product=>`<div class="card"><div class="productImage"><img src="${product.image}" alt="${product.name}"></div><h3>${product.name}</h3><p class="price">${formatNaira(product.price)}</p><p class="stockText">Available Stock: ${product.stock}</p><button class="btn" onclick="addToCart(${product.id})">Add to Cart</button></div>`).join("")}function addToCart(id){const product=products.find(p=>p.id===id);const existing=cart.find(item=>item.id===id);if(existing){existing.quantity+=1}else{cart.push({...product,quantity:1})}renderCart()}function removeFromCart(id){cart=cart.filter(item=>item.id!==id);renderCart()}function renderCart(){if(cart.length===0){cartItems.innerHTML="<p>Your cart is empty.</p>";cartTotal.textContent=formatNaira(0);return}cartItems.innerHTML=cart.map(item=>`<div class="cartItem"><div><strong>${item.name}</strong><br>${formatNaira(item.price)} x ${item.quantity}</div><button onclick="removeFromCart(${item.id})">Remove</button></div>`).join("");const total=cart.reduce((sum,item)=>sum+item.price*item.quantity,0);cartTotal.textContent=formatNaira(total)}document.querySelectorAll(".filter").forEach(button=>{button.addEventListener("click",()=>{document.querySelectorAll(".filter").forEach(b=>b.classList.remove("active"));button.classList.add("active");currentCategory=button.dataset.category;renderProducts()})});searchInput.addEventListener("input",renderProducts);document.getElementById("checkoutForm").addEventListener("submit",function(e){e.preventDefault();if(cart.length===0){alert("Please add at least one item to cart.");return}const name=document.getElementById("customerName").value;const phone=document.getElementById("customerPhone").value;const address=document.getElementById("customerAddress").value;const fulfillment=document.getElementById("fulfillment").value;const paymentMethod=document.getElementById("paymentMethod").value;const total=cart.reduce((sum,item)=>sum+item.price*item.quantity,0);const orderList=cart.map(item=>`- ${item.name} x ${item.quantity} = ${formatNaira(item.price*item.quantity)}`).join("%0A");const message=`Hello Biserry Groceries,%0A%0AI want to place an order.%0A%0AName: ${name}%0APhone: ${phone}%0AOrder Type: ${fulfillment}%0APayment Method: ${paymentMethod}%0AAddress/Note: ${address}%0A%0AItems:%0A${orderList}%0A%0ATotal: ${formatNaira(total)}`;window.open(`https://wa.me/2348100584211?text=${message}`,"_blank")});renderProducts();renderCart();